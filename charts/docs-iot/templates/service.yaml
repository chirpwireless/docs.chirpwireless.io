apiVersion: v1
kind: Service
metadata:
  name: {{ include "docs-iot.fullname" . }}
  labels:
    {{- include "docs-iot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docs-iot.selectorLabels" . | nindent 4 }}
