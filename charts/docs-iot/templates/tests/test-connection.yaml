apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docs-iot.fullname" . }}-test-connection"
  labels:
    {{- include "docs-iot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docs-iot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
